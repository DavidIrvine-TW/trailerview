// 'use client'

// export function toggleBookmark(card, isBookmarked, addBookmark, removeBookmark) {
//     if (isBookmarked) {
//       removeBookmark(card.id);
//     } else {
//       addBookmark(card);
//     }
//   }
