/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["image.tmdb.org", "avatars.githubusercontent.com", "lh3.googleusercontent.com"],
    },
}

module.exports = nextConfig
